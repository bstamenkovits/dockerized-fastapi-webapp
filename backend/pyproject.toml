[project]
name = "dockerized-fastapi-webapp"
version = "1.0.0"
description = ""
requires-python = ">=3.8"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

# [project.scripts]
# dockerized-fastapi-webapp = "backend.src.app:app"
